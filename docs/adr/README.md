# ADR (Architecture Decision Records)

Ten katalog zbiera krótkie notatki o decyzjach architektonicznych, które mają wpływ na rozwój i utrzymanie Pipe.

## Spis

- `docs/adr/0001-bundling-pipe-py.md` – dlaczego `src/*` + generowany `pipe.py`

